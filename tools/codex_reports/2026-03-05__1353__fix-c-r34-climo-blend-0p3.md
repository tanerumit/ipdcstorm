# Fix C: Reduce climo-R34 calibration blend weight to 0.3

## Goal
Implement Fix C by changing only the climatological R34 calibration blend weight from `0.5` to `0.3` in the effective windfield calibration pathway, then validate workflows in current dirty workspace state.

## Scope
- Intended scope per task: authoritative R34 blending site in windfield calculations.
- Actual code edit: `R/hazard_core.R` only.
- No API/signature/return-structure changes.
- No dependency changes.

## Summary
- Updated the climo calibration blend in `.estimate_site_wind_holland()`:
  - from: `cal_factor[blend] <- 1 + 0.5 * (cal_factor[blend] - 1)`
  - to:   `cal_factor[blend] <- 1 + 0.3 * (cal_factor[blend] - 1)`
- Kept change at a single authoritative site; did not modify lambda scaling, outer cutoff, gamma/steepening, or public interfaces.

## Files Changed
- `R/hazard_core.R`
- Report file: `tools/codex_reports/2026-03-05__1353__fix-c-r34-climo-blend-0p3.md`

## Commands Run
- `Rscript -e "parse(file='R/hazard_core.R')"`
- `Rscript -e "devtools::test()"`
- Full validation attempt (wrapper):
  - `validate_hazard_model(...)` with `val_cfg$output$level <- 'full'`
- Full validation with diagnostics target injection:
  - `out <- run_hazard_model(...)`
  - `out$targets <- targets`
  - `run_validation_suite(out = out, cfg = val_cfg)`

## Test Results
- Parse check:
  - `R/hazard_core.R`: PASS
- Test suite:
  - `devtools::test()`: PASS (`51` passed, `0` failed, `0` warned, `0` skipped)

## Manual Validation (Full Mode, DIAGNOSTICS A–D)
- In current dirty state, `validate_hazard_model()` path printed:
  - `DIAGNOSTICS A–D skipped: targets not available in run_validation_suite()`
- With explicit `out$targets <- targets`, DIAGNOSTICS A–D printed and showed:
  - `mult_obs=1.50; mult_climo=1.50` (unchanged)
  - Metric C (dist/R34_p50_top1):
    - `St_Martin = 0.270`
    - `Saba = 0.321`
    - `Statia = 0.325`
- Tier 1A (same run):
  - Statia remained positively biased at RP25/RP50 (e.g., +21%, +25% in printed table).
  - St_Martin did not show strong negative bias (remained slight positive bias in RP25/RP50).

## Behavior Changes
- Intended/implemented behavior change:
  - climo-R34 calibration blend weight reduced from `0.5` to `0.3` at the authoritative calibration blend site.
- No other intended behavior changes introduced by this patch.

## Follow-ups / Risks
- Dirty-state workflow risk observed:
  - Existing workspace behavior currently allows DIAGNOSTICS A–D to be skipped in `validate_hazard_model()` path due missing targets in `run_validation_suite()` context.
  - This is pre-existing to this patch and can hide required diagnostics unless targets are injected or passed through.
- Calibration impact risk:
  - In this run, DIAGNOSTICS C did not show the target +0.05 to +0.10 increase for Saba/Statia; values remained around `0.32` (geometry still permissive).
  - This suggests Fix C at this site is active but may have limited leverage on top-1% geometry under current data/pathway mix.

## A/B Quantification (exact, same workspace state)

Method:
- Ran two controlled passes with identical inputs/seeds and extraction code.
- A = climo blend weight `0.5`
- B = climo blend weight `0.3`
- Extracted from:
  - `diag_tbl <- .run_validation_diagnostics_ad(out$trackpoints, targets)`
  - `hc <- .validate_hindcast_all(...)` and `hc$comparison`

Exact extracted values (B = 0.3):
- `dist/R34_p50_top1`
  - St_Martin: `0.270325113982096`
  - Saba: `0.321362420146243`
  - Statia: `0.325175501927390`
- Tier 1A `bias_pct`:
  - St_Martin RP25: `2.36315743007520`
  - St_Martin RP50: `3.29750785387435`
  - Statia RP25: `21.2867009244670`
  - Statia RP50: `25.0434028632297`

Exact extracted values (A = 0.5):
- `dist/R34_p50_top1`
  - St_Martin: `0.270325113982096`
  - Saba: `0.321362420146243`
  - Statia: `0.325175501927390`
- Tier 1A `bias_pct`:
  - St_Martin RP25: `-1.08590019719500`
  - St_Martin RP50: `-0.717643260950831`
  - Statia RP25: `21.3069814161248`
  - Statia RP50: `25.0714784915351`

Exact deltas (B - A):
- DIAGNOSTICS C `dist/R34_p50_top1`
  - St_Martin: `+0.000000000000000`
  - Saba: `+0.000000000000000`
  - Statia: `+0.000000000000000`
- Tier 1A `bias_pct` (percentage points)
  - St_Martin RP25: `+3.44905762727020`
  - St_Martin RP50: `+4.01515111482518`
  - Statia RP25: `-0.02028049165780`
  - Statia RP50: `-0.02807562830540`

Interpretation from A/B:
- In this deterministic A/B run, Fix C produced **no measurable change** in DIAGNOSTICS C ratio for Saba/Statia/St_Martin.
- Statia RP25/RP50 bias moved slightly downward (improvement is very small).
- St_Martin RP25/RP50 bias moved upward by ~3.45 to ~4.02 pp.
