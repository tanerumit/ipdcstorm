# Zero-row sampled-events tibble

Returns a zero-row tibble with the schema expected by
[`generate_daily_year_extended()`](https://tanerumit.github.io/ipdcstorm/reference/generate_daily_year_extended.md).

## Usage

``` r
.empty_sampled_events_tbl()
```

## Value

Zero-row tibble.
