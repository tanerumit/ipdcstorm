# Normalise storm-class selection.

Resolves a user-supplied storm-class vector to a unique character vector
with defaults when missing.

## Usage

``` r
.normalize_storm_classes(storm_classes = NULL)
```

## Arguments

- storm_classes:

  Character vector of storm classes to retain.

## Value

Character vector of unique storm classes.
