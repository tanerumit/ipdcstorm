# Normalize lambda scaling mode

Normalize lambda scaling mode

## Usage

``` r
.normalize_lambda_scaling_mode(mode)
```

## Arguments

- mode:

  Character scalar mode.

## Value

Character scalar in "target", "down_only".
