# Default storm-perturbation parameters for climate sensitivity

Returns a named list of per-degC scaling factors for storm-property
perturbation.

## Usage

``` r
default_cc_params()
```

## Value

Named list with elements v_scale, r_scale, speed_scale, precip_scale.
