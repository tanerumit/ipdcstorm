# Return Holland outer-cutoff multipliers.

Returns the deterministic multipliers used to convert `R34_km` into the
outer cutoff radius for the Holland profile.

## Usage

``` r
.holland_outer_cutoff_multipliers()
```

## Value

Named list with `observed` and `climo` multipliers.
