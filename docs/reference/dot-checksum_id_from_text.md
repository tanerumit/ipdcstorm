# Deterministic checksum ID from text

Deterministic checksum ID from text

## Usage

``` r
.checksum_id_from_text(text, prefix = "id")
```

## Arguments

- text:

  Character vector.

- prefix:

  Character scalar prefix for the ID.

## Value

Character scalar deterministic ID.
