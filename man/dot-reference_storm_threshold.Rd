% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_query.R
\name{.reference_storm_threshold}
\alias{.reference_storm_threshold}
\title{Derive an impact threshold for a reference storm}
\usage{
.reference_storm_threshold(daily, out, storm_id, metric, location = NULL)
}
\arguments{
\item{daily}{Flat tibble (already resolved by \code{.resolve_daily_tbl()}).}

\item{out}{List returned by \code{run_hazard_model()}.}

\item{storm_id}{Character scalar IBTrACS SID of the reference storm.}

\item{metric}{Character scalar metric name; one of \code{"peak_wind_kt"},
\code{"cum_damage"}, or \code{"max_damage_rate"}.}

\item{location}{Character vector of locations to consider, or \code{NULL}
for all.}
}
\value{
Named numeric vector of thresholds, one entry per location.
}
\description{
Resolves a numeric threshold for a named metric from either:
\enumerate{
\item The historical event record in \code{out$events} (for
\code{metric = "peak_wind_kt"}).
\item The rows of \code{daily} where that storm appears as the sampled
event (for damage metrics), taking the median annual metric across
those occurrences.
}
}
\keyword{internal}
