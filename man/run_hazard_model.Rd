% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_run.R
\name{run_hazard_model}
\alias{run_hazard_model}
\title{Run hazard model for multiple target locations}
\usage{
run_hazard_model(
  cfg,
  targets,
  per_target_cfg = list(),
  severities = c("TS", "HUR"),
  sst_cfg = NULL
)
}
\arguments{
\item{cfg}{Hazard configuration from \code{make_hazard_cfg()}.}

\item{targets}{Data frame/tibble with columns: name, lat, lon.}

\item{per_target_cfg}{Named list of per-target options (currently unused).}

\item{severities}{Character vector of storm classes to model (default TS and HUR).}

\item{sst_cfg}{Optional SST configuration from \code{make_sst_cfg()}. When provided
and enabled, the annual count simulation uses SST-conditioned rate scaling.}
}
\value{
A list with elements:
\code{sim}, \code{events}, \code{trackpoints}, \code{rates}, \code{fit},
and \code{cfg}.
}
\description{
End-to-end workflow:
\enumerate{
\item Read IBTrACS and filter to basin/season
\item Gate track points within cfg$search_radius_km of each target
\item Compute site winds and storm event summaries
\item Classify storm classes and compute annual rates
\item Optionally estimate beta_SST from historical SST-activity relationship
\item Simulate annual counts with SST-conditioned rate scaling (Level 1 climate mod)
}
}
