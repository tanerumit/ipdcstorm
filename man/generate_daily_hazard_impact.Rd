% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_downscale.R
\name{generate_daily_hazard_impact}
\alias{generate_daily_hazard_impact}
\title{Generate daily synthetic hazard + impact series from hazard model output}
\usage{
generate_daily_hazard_impact(
  out,
  location,
  sim_years = 1:1000,
  year0 = 2000,
  gust_factor = 1,
  damage_method = c("intensity", "powerlaw"),
  damage_params = list(),
  pulse_shape = "cosine",
  scenario = NA_character_,
  seed = 1
)
}
\arguments{
\item{out}{List returned by \code{run_hazard_model()}.}

\item{location}{Character vector of one or more target location names.}

\item{sim_years}{Integer vector of simulation-year indices to generate.}

\item{year0}{Integer base calendar year corresponding to sim_year == 1.}

\item{gust_factor}{Numeric scalar to derive gust wind from sustained wind.}

\item{damage_method}{One of: "intensity" (uses add_damage_forcing) or "powerlaw".}

\item{damage_params}{List of parameters for the chosen method.}

\item{pulse_shape}{Passed to \code{event_pulse()}.}

\item{scenario}{Optional character scenario label carried to output.}

\item{seed}{Integer seed.}
}
\value{
Named list of tibbles (one per location), each with columns:
\code{location}, \code{sim_year}, \code{scenario}, \code{date},
\code{wind_kt}, \code{wind_gust_kt}, \code{surge_m},
\code{event_id}, \code{event_class}, \code{pressure_hpa},
\code{pressure_deficit_hpa}, \code{rmw_km},
\code{damage_intensity}, \code{damage_rate}, \code{cum_damage}.
}
\description{
For one or more locations and simulated years, creates daily hazard and damage
series with a fixed 15-column schema.  Always returns a \strong{named list} of
tibbles, one element per location (e.g. \code{out$Saba}, \code{out$Miami}).
}
\examples{
\dontrun{
# Single location — returns list of length 1
res <- generate_daily_hazard_impact(out, location = "Saba")
res$Saba

# Multiple locations
res <- generate_daily_hazard_impact(out, location = c("Saba", "St. Eustatius"))
res$Saba
res$`St. Eustatius`
}
}
