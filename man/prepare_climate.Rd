% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_climate.R
\name{prepare_climate}
\alias{prepare_climate}
\title{Load and prepare climate data based on configuration}
\usage{
prepare_climate(
  climate_cfg,
  annual_counts = NULL,
  lambda_table = NULL,
  min_year = 1970L,
  verbose = TRUE
)
}
\arguments{
\item{climate_cfg}{List from \code{make_climate_cfg()}.}

\item{annual_counts}{Optional tibble of annual counts for rate and intensity estimation.}

\item{lambda_table}{Optional tibble from \code{compute_lambda_table()} for p_HUR_base.}

\item{min_year}{Integer; passed to estimation functions.}

\item{verbose}{Logical.}
}
\value{
A list with:
\item{sst_df}{Tibble of historical SST with anomalies.}
\item{beta_sst}{Estimated or user-supplied rate-effect coefficient.}
\item{beta_info}{Full output from \code{estimate_beta_sst()} (or NULL).}
\item{gamma}{Estimated or user-supplied intensity-effect coefficient.}
\item{gamma_info}{Full output from \code{estimate_gamma_intensity()} (or NULL).}
\item{p_hur_base}{Baseline hurricane fraction.}
\item{perturb}{Resolved storm-perturbation parameters (or \code{NULL}).}
\item{climate_cfg}{The input configuration.}
}
\description{
Reads SST data from the configured source, computes anomalies, and
optionally estimates the rate effect and intensity effect. Returns a
processed climate object ready for use in the hazard model.
}
\examples{
cfg <- make_climate_cfg(enabled = TRUE, sst_source = "builtin", scenario = "stationary")
prep <- prepare_climate(cfg, verbose = FALSE)
prep$beta_sst

}
