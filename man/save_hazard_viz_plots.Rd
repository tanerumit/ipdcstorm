% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_viz.R
\name{save_hazard_viz_plots}
\alias{save_hazard_viz_plots}
\title{Save standard hazard visualization plots as PNG files}
\usage{
save_hazard_viz_plots(
  daily,
  output_dir,
  location_name,
  width = 9,
  height = 6,
  dpi = 300,
  base_size = 11,
  thr_tc = 34,
  thr_hur = 64
)
}
\arguments{
\item{daily}{A data frame/tibble or named list of daily tables. Inputs must
contain the columns required by \code{plot_monthly_events()},
\code{plot_annual_counts()}, \code{plot_wind_timeseries()},
\code{plot_wind_distribution()}, and \code{plot_intensity_duration()}. Named lists
are flattened using the list names as \code{location}.}

\item{output_dir}{Character scalar path to the folder where PNG files are
saved. The directory is created if it does not already exist.}

\item{location_name}{Character scalar used in standardized plot titles.}

\item{width}{Numeric scalar plot width in inches passed to \code{ggsave()}.}

\item{height}{Numeric scalar plot height in inches passed to \code{ggsave()}.}

\item{dpi}{Numeric scalar resolution passed to \code{ggsave()}.}

\item{base_size}{Numeric scalar base font size applied via \code{plot_theme()}.}

\item{thr_tc}{Numeric scalar; tropical-cyclone threshold in knots.}

\item{thr_hur}{Numeric scalar; hurricane threshold in knots.}
}
\value{
A named list with components \code{plots} and \code{paths}.
}
\description{
Builds a standard set of five hazard plots, applies consistent
titles, subtitles, and base theme sizing, and saves each plot to \code{output_dir}
as a PNG file.
}
\examples{
\dontrun{
d <- data.frame(
  date = as.Date("2001-01-01") + 0:364,
  wind_kt = pmax(1, 20 + rnorm(365, sd = 8)),
  event_id = sample(c(NA, 1:8), 365, replace = TRUE),
  location = "A",
  sim_year = 2001
)
save_hazard_viz_plots(d, tempdir(), "A")
}
}
