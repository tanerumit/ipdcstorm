% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_viz.R
\name{plot_doy_wind}
\alias{plot_doy_wind}
\title{Plot mean and maximum wind by day of year}
\usage{
plot_doy_wind(daily, smooth = TRUE, span = 0.15, thr_tc = 34, thr_hur = 64)
}
\arguments{
\item{daily}{A data frame or tibble with columns \code{date} (\code{Date}) and
\code{wind_kt} (numeric, knots).}

\item{smooth}{Logical scalar; if \code{TRUE}, use \code{geom_smooth(method = "loess")},
otherwise plot unsmoothed lines.}

\item{span}{Numeric scalar in \verb{(0, 1]}; loess span used when \code{smooth = TRUE}.}

\item{thr_tc}{Numeric scalar; tropical-cyclone threshold in knots.}

\item{thr_hur}{Numeric scalar; hurricane threshold in knots.}
}
\value{
A \code{ggplot} object.
}
\description{
Aggregates daily wind by day-of-year and plots mean and maximum
wind speed curves with optional loess smoothing.
}
\details{
Summary statistics use \code{na.rm = TRUE}; all-\code{NA} day groups propagate
non-finite summaries. Threshold lines are always drawn at \code{thr_tc} and
\code{thr_hur}.
}
\examples{
d <- data.frame(
  date = as.Date("2001-01-01") + 0:29,
  wind_kt = 15 + sin((1:30) / 5) * 10
)
plot_doy_wind(d, smooth = FALSE)
}
