% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_downscale.R
\name{.build_event_rows_from_sids}
\alias{.build_event_rows_from_sids}
\title{Build event rows from shared SIDs using a location-specific library}
\usage{
.build_event_rows_from_sids(lib, sids, sev, year, counter_start)
}
\arguments{
\item{lib}{Event library from \code{build_event_library_from_out()}.}

\item{sids}{Character vector of SIDs to look up.}

\item{sev}{Character scalar severity; one of \code{"TS"} or \code{"HUR"}.}

\item{year}{Integer scalar calendar year for date construction and
\code{event_id} encoding.}

\item{counter_start}{Integer scalar starting value for the within-year event
counter (used to produce unique \code{event_id} values across TS and HUR
draws for the same year and location).}
}
\value{
Named list with elements:
\describe{
\item{\code{rows}}{Tibble of event rows (zero or more).}
\item{\code{n_filled}}{Integer count of rows produced.}
\item{\code{counter}}{Updated event counter.}
}
}
\description{
For each SID in \code{sids} that exists in \code{lib$events}, constructs one
event row in the schema produced by \code{sample_events_for_year_extended()}.
SIDs absent from the location library are silently skipped, so the number of
rows returned may be less than \code{length(sids)}.
}
\keyword{internal}
