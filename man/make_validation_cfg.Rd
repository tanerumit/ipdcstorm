% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_validation.R
\name{make_validation_cfg}
\alias{make_validation_cfg}
\title{Create a validation configuration}
\usage{
make_validation_cfg(
  holdout_years = 10L,
  n_sim = NULL,
  return_periods = c(5, 10, 25, 50),
  conf_level = 0.95,
  tier1_pass_rate = 0.8,
  full_output = FALSE,
  seed = 42L,
  out_dir = "output/validation",
  save_plots = TRUE,
  save_tables = TRUE,
  advanced = NULL
)
}
\arguments{
\item{holdout_years}{Integer; number of years to hold out from the end of
the historical record for train/test split (default: 10).}

\item{n_sim}{Integer or \code{NULL}; number of synthetic years to simulate for
hindcast comparison. If \code{NULL} (default), \code{run_validation_suite()}
inherits the simulation length from the hazard model output.}

\item{return_periods}{Numeric vector of return periods (years) to compare
(default: 5, 10, 25, 50).}

\item{conf_level}{Numeric; confidence level for return-level CIs
(default: 0.90). Must be in \verb{(0, 1)}. This controls the width of the
confidence interval around GEV-based return level estimates, computed
via the delta method or parametric bootstrap of the hurdle-GEV model.
Common choices: 0.90 (standard), 0.95 (stricter), 0.80 (exploratory).
\strong{Note}: this quantifies estimation uncertainty in return levels given
the observed record length --- it is not a prediction interval for
future storm intensities.}

\item{tier1_pass_rate}{Numeric; minimum fraction of return-period/location
comparisons that must fall within the CI for Tier 1 to show ✓ in the
summary (default: 0.80). A value of 1.0 restores the original strict
all-must-pass behaviour. Must be in \verb{(0, 1]}.}

\item{full_output}{Logical; when \code{FALSE} (default) \code{run_validation_suite()}
returns a concise list (\code{summary}, \code{comparison}, \code{rate_check},
\code{wind_field}, \code{plots}). When \code{TRUE}, the full internal objects are also
returned (\code{hindcast}, \code{climate_response}, \code{artifacts}). The slim output
is sufficient for most diagnostics; use \code{full_output = TRUE} when you need
the per-island breakdown or to re-generate plots from the returned object.}

\item{seed}{Integer; random seed for reproducibility.}

\item{out_dir}{Character; output directory for saved plots and tables.}

\item{save_plots}{Logical; whether to save standard validation figures.}

\item{save_tables}{Logical; whether to save CSV + markdown tables.}

\item{advanced}{Optional named list of expert parameters. Most users should
leave this as \code{NULL}. Supported names:
\describe{
\item{\code{xi_bounds}}{Numeric vector of length 2; allowed range for GEV
shape parameter (default: \code{c(-0.3, 0.4)}).}
\item{\code{base_size}}{Numeric; base font size for ggplot themes
(default: 11).}
\item{\code{hindcast_use_raw_rates}}{Logical; whether Tier 1A hindcast uses
raw fitted rates instead of adjusted rates (default: \code{TRUE}). This is
the validated package default because adjusted lambda worsened
hindcast bias at Saba and Statia.}
}

The default validation path is the current best validated overall tradeoff:
Tier 1A hindcast uses raw lambda, the package runs the legacy wind-field
path unless an internal option overrides it, and the hindcast sampler
defaults to \code{legacy}. Experimental wind-field comparison modes remain
internal and are never activated by \code{make_validation_cfg()} defaults.}
}
\value{
A list with class \code{c("validation_cfg", "list")}.
}
\description{
Creates a typed configuration object for \code{run_validation_suite()}.
Follows the same pattern as \code{make_hazard_cfg()} and \code{make_sst_cfg()}:
common parameters up front, expert-only knobs in \code{advanced}.
}
\examples{
# Defaults - suitable for most users
val_cfg <- make_validation_cfg()

# Custom holdout and output location
val_cfg <- make_validation_cfg(holdout_years = 15, out_dir = "results/val")

# Expert tuning
val_cfg <- make_validation_cfg(
  n_sim = 10000,
  advanced = list(xi_bounds = c(-0.4, 0.5), base_size = 13)
)
}
