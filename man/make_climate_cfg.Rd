% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_climate.R
\name{make_climate_cfg}
\alias{make_climate_cfg}
\title{Build a climate configuration object for the hazard model}
\usage{
make_climate_cfg(
  scenario = "stationary",
  sst_source = c("builtin", "csv", "ersst_nc"),
  sst_path = NULL,
  baseline_years = 1991L:2020L,
  start_year = 2025L,
  delta_sst = NULL,
  target_year = NULL,
  sensitivity_mode = c("fixed", "linear_shifted"),
  k_beta = 0,
  k_gamma = 0,
  perturb = NULL
)
}
\arguments{
\item{scenario}{Optional character; climate scenario name used for helper
resolution and provenance metadata.}

\item{sst_source}{Character; one of "builtin", "csv", "ersst_nc".}

\item{sst_path}{Optional character; path to SST data file (CSV or NetCDF).}

\item{baseline_years}{Integer vector; years for climatological baseline.}

\item{start_year}{Integer; first year of the simulation scenario.}

\item{delta_sst}{Optional numeric scalar explicit SST shift in degC.}

\item{target_year}{Optional numeric scalar target year used to derive
scenario-helper \code{delta_sst}.}

\item{sensitivity_mode}{Character scalar; one of \code{"fixed"} or
\code{"linear_shifted"}.}

\item{k_beta}{Numeric scalar; linear sensitivity-shift coefficient applied
to \code{beta_0} when \code{sensitivity_mode = "linear_shifted"}.}

\item{k_gamma}{Numeric scalar; linear sensitivity-shift coefficient applied
to \code{gamma_0} when \code{sensitivity_mode = "linear_shifted"}.}

\item{perturb}{Controls storm-perturbation of individual event properties
(intensity, duration, wind radii) by \code{delta_sst}.
\itemize{
\item \code{TRUE}: enable with scenario-appropriate defaults (recommended for KNMI scenarios).
\item \code{FALSE} or \code{NULL}: disable (default).
\item named list: enable with explicit parameter overrides (see \code{default_cc_params()}).
}}
}
\value{
A list with class "climate_cfg" containing climate configuration parameters.
}
\description{
Creates a climate configuration object for \code{run_hazard_model()}.
The climate workflow first calibrates historical baseline sensitivities
(\code{beta_0}, \code{gamma_0}) from historical annual counts and MDR SST anomalies,
then resolves scenario-specific simulation scalars from \code{delta_sst}.
By default the historical sensitivities are used unchanged. Expert users may
instead enable a linear sensitivity shift:

\code{beta_sst = beta_0 * (1 + k_beta * delta_sst)}

\code{gamma = gamma_0 * (1 + k_gamma * delta_sst)}

Optional storm perturbation shifts individual event intensity, duration, and
wind radii by \code{delta_sst}. It is disabled by default; pass \code{perturb = TRUE}
to enable scenario-appropriate defaults.

Climate can be specified in two equivalent ways:
\enumerate{
\item Scenario helper mode: provide \code{scenario} plus \code{target_year} to derive
\code{delta_sst}.
\item Direct mode: provide \code{delta_sst} explicitly.
}

After \code{delta_sst} is resolved, downstream hazard logic depends only on the
resolved \code{delta_sst} and fixed model settings. Scenario names and timing are
retained only as provenance metadata. If both direct \code{delta_sst} and
scenario-derived inputs are supplied, they must resolve to the same
\code{delta_sst} or an error is raised.

If \code{scenario = "stationary"} and \code{delta_sst} is not supplied, the returned
config represents the canonical baseline hazard-model specification with
\code{delta_sst = 0}. Climate is always resolved through this configuration; there
is no disabled climate mode.
}
\examples{
cfg <- make_climate_cfg(
  sst_source = "builtin",
  scenario = "ssp245",
  target_year = 2050
)
cfg_direct <- make_climate_cfg(delta_sst = 0.5)
cfg
}
