% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard_climate.R
\name{make_climate_cfg}
\alias{make_climate_cfg}
\title{Build a climate configuration object for the hazard model}
\usage{
make_climate_cfg(
  enabled = TRUE,
  scenario = "stationary",
  sst_source = c("builtin", "csv", "ersst_nc"),
  sst_path = NULL,
  baseline_years = 1991L:2020L,
  start_year = 2025L,
  beta_sst = NULL,
  gamma = NULL,
  perturb = NULL
)
}
\arguments{
\item{enabled}{Logical; whether to enable climate adjustments.}

\item{scenario}{Character; climate scenario name.}

\item{sst_source}{Character; one of "builtin", "csv", "ersst_nc".}

\item{sst_path}{Optional character; path to SST data file (CSV or NetCDF).}

\item{baseline_years}{Integer vector; years for climatological baseline.}

\item{start_year}{Integer; first year of the simulation scenario.}

\item{beta_sst}{Optional numeric scalar. If \code{NULL}, the rate effect is
estimated from data during \code{prepare_climate()}.}

\item{gamma}{Optional numeric scalar. If \code{NULL}, the intensity effect is
estimated from data during \code{prepare_climate()}.}

\item{perturb}{Optional storm-perturbation settings.
\itemize{
\item \code{NULL}: disable storm perturbation.
\item \code{list()}: enable storm perturbation with defaults from \code{default_cc_params()}.
\item named list: enable storm perturbation with user overrides.
}}
}
\value{
A list with class "climate_cfg" containing climate configuration parameters.
}
\description{
Creates a climate configuration object for \code{run_hazard_model()}.
The default climate-adjusted workflow combines a rate effect (\code{beta_sst})
and an intensity effect (\code{gamma}) derived from MDR SST anomalies.
Optional storm perturbation is available as an expert sensitivity and is
disabled unless \code{perturb} is supplied explicitly.

\code{scenario} accepts \code{"stationary"} plus any value returned by
\code{sst_scenario_info("all")$scenario} (for example SSP and optional KNMI
scenarios when KNMI helpers are available).

If \code{scenario = "stationary"}, the returned config forces
\code{enabled = FALSE} so the model behaves as a stationary run.
}
\examples{
cfg <- make_climate_cfg(
  enabled = TRUE,
  sst_source = "builtin",
  scenario = "ssp245"
)
cfg
}
